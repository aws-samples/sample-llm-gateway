# IRSA：role-arn 注解由 `eksctl create iamserviceaccount` 写入（见 README）。
apiVersion: v1
kind: ServiceAccount
metadata:
  name: llm-gateway
  namespace: llm-gateway
  annotations:
    eks.amazonaws.com/role-arn: "arn:aws:iam::123456789012:role/llm-gateway-poc-bedrock-invoke"
